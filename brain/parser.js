var _ = require('underscore');
var mustache = require('mustache');
var labels = require('./labels');

var Parser = (function () {
    function Parser(topics, content) {
        
    };
    return Parser;
})();